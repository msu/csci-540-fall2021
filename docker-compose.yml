version: "3.7"

services:
  pgdb:
    build: env/postgres
    image: pgdb
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=pw

  client:
    depends_on:
      - pgdb
    build: env/client
    image: client
    volumes:
      - $PWD/tmp:/mount
